# 舞艺嘉学校 · 项目记录

> 更新时间：2026-08-03　｜　项目：`wuyijiaschool/pwa`

## 项目简介

「舞艺嘉学校」是一套面向舞蹈培训机构的轻量教务管理系统，以 PWA（渐进式 Web 应用）形态交付，可安装到手机主屏幕，离线可用。

核心能力覆盖教务日常：

- **考勤管理**：每日出勤/请假标记、一键全部出勤、日历标记（紫=出勤、红=请假、蓝=作业）
- **作业管理**：每日作业提交状态登记
- **课时卡记账**：购卡、按 FIFO 扣课、欠课记账（用完后可继续出勤，买新卡自动抵消）
- **班级/学员管理**：班级绑定课程、学员多班制、学员个人日历（考勤+作业）
- **报表导出**：班级汇总、学员明细，导出 Excel（HTML 格式，支持颜色）
- **多数据版本**：每位老师独立数据空间，切换需管理员密码

## 技术栈

| 类别 | 选型 |
| --- | --- |
| 框架 | React 18 + TypeScript |
| 构建 | Vite 7 |
| 样式 | 原生 CSS（自定义设计变量，无 UI 框架） |
| 存储 | Supabase（云端）+ IndexedDB（本地备份，自动恢复） |
| 部署 | GitHub Pages + Cloudflare Pages + Gitee Pages |
| 离线 | Service Worker（网络优先策略） |

## 目录结构

```
pwa/
├── index.html              # 入口 + 预加载启动屏
├── public/                 # 图标、manifest、Service Worker
│   └── sw.js               # 缓存策略（JS/CSS 网络优先，导航网络优先）
├── src/
│   ├── main.tsx            # 入口，注册 SW
│   ├── App.tsx             # 启动屏 → 引导页 → 主界面路由
│   ├── styles.css          # 全局设计系统（极简风设计变量）
│   ├── types.ts            # 领域模型（班级/学员/课时卡/考勤/作业）
│   ├── storage.ts          # IndexedDB 本地备份与恢复
│   ├── supabase.ts         # 云端同步
│   ├── utils.ts            # 日期、CSV/Excel 导出、课时卡排序
│   ├── context/AppContext.tsx  # 全局状态与业务动作
│   ├── components/         # Layout、Calendar、Splash、Sheet 等
│   └── pages/              # Dashboard / Classes / Reports / Settings
├── scripts/                # 备份、部署、数据导入脚本
└── supabase-backup/        # 云端数据备份快照
```

## 开发时间线（git 记录）

| 日期 | 内容 |
| --- | --- |
| 2026-06-04 | 初始版本，适配 GitHub Pages 基础路径 |
| 2026-06-05 | 工作区系统（每人独立数据版本）；Vercel 部署兼容；SW 缓存策略 |
| 2026-06-07 | v2.0 大版本：工作台重构 + 全局个人日历浮层 + 启动屏；UI 全面精炼 |
| 2026-06-08 | v2.1：新设备引导 + 删除备份提示；学员日历考勤/作业合并标签页 |
| 2026-06-12 | 课程管理优化（锁定班级课程绑定、重命名）；数据版本重命名；考勤移除旷课、请假标红；报表 CSV 分组 + 课时卡动态剩余 |
| 2026-06-13 | 考勤列表请假红点；SW 缓存 v3→v4 |
| 2026-07-01 | Supabase 数据兼容性校验，防止其他 PWA 写入脏数据 |
| 2026-07-14 | 数据持久化（IndexedDB + Supabase 保活）；PWA 数据隔离（app_name）；课时卡欠课记账；考勤切换优化；SW v4→v5；Cloudflare Pages 部署 |
| 2026-07-15 | 删班级同步清理课程卡；导出改 Excel 格式支持颜色 |
| 2026-07-17 | 日历标记区分出勤紫/请假红，取消考勤后正确消失 |
| 2026-08-03 | 去掉启动屏跳舞小女孩动图；整体改为 Codex 极简风；修复设置页课程列表 hooks 隐患；界面继续贴近 iOS/Safari 原生风格（隐藏描边线条、灰填充控件、发丝分隔线）；去掉各页顶部大标题（设置页改为品牌名）、工作台日历加小标题并简化翻页按钮；字体改为系统原生优先（iOS 为 SF Pro）、弹层毛玻璃材质、select 回归系统原生控件；模拟 iOS Liquid Glass 质感（氛围光背景 + 磨砂玻璃面板 + 弹簧动效与按压反馈 + 支持减弱动态效果） |

## 部署方式

- **GitHub Pages**：推送到 `main` 后由 `.github/workflows/deploy.yml` 自动构建部署
- **Cloudflare Pages**（https://wuyijiaschool.pages.dev）：关联仓库自动部署
- **Gitee**：`scripts/deploy-gitee.sh` 手动部署
- 构建命令：`npm run build`，产物输出到 `dist/`

## 数据与存储

- 数据按 `workspaces` 隔离，每个版本含完整的教务数据（`app_name` 字段防止跨 PWA 串数据）
- 写入同时更新 Supabase；本地 IndexedDB 兜底，云端不可用时自动恢复本地数据
- `scripts/backup-supabase.sh` 可手动拉取云端备份

## 常用命令

```bash
npm install      # 安装依赖
npm run dev      # 本地开发
npm run build    # 生产构建
```

## 待办 / 后续方向

- [ ] 考勤统计趋势（周/月对比）
- [ ] 多管理员账号与权限
- [ ] 深色模式
- [ ] 消息通知（上课提醒）
